import { Group } from "@mantine/core";

export default function EmailAction(): React.ReactNode {
    return (
        <>
            <Group style={{justifyContent: 'center'}}>
                Email
            </Group>
        </>
    );
}